#!/bin/sh

# Run full verification suite (blocking)
echo "Running verification suite..."
powershell -File scripts/verify.ps1 || exit 1

# Update AGENTS.md
if command -v compose-agentsmd > /dev/null 2>&1; then
  echo "Updating AGENTS.md..."
  compose-agentsmd --compose
  git add AGENTS.md
fi
