name: verify

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: programming-course-docs-verify-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Cache npm tool downloads
        uses: actions/cache@v5
        with:
          path: |
            ~/.npm
            ~/AppData/Local/npm-cache
          key: ${{ runner.os }}-programming-course-docs-verify-tools-${{ hashFiles('.github/workflows/verify.yml', 'package-lock.json', 'scripts/verify.ps1') }}
      - name: Install dependencies
        run: npm ci
      - name: Run verification script
        run: powershell -File scripts/verify.ps1
