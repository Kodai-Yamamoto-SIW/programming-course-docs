---
title: カードを作ろう
authoringMode: tutorial
---

今日は、商品カードを少しずつ作ります。先生と同じように、コードを書きながら見た目が変わるところを確認しましょう。

このレッスンでは、次の3つを覚えます。

- **HTML の `class` と CSS の `.名前` を対応させて、一部分だけを選ぶ**
- ボックスモデルの 4 つの層（content / padding / border / margin）の役割
- `width`・`height`・`border`・`padding` の 4 つのプロパティでカードの見た目を作る

<Section title="完成形を見る" goal="これから作る商品カードの見た目を先に確認する">

まずは完成形だけ見ます。ここでは、まだコードは書きません。

<CodePreview
    previewVisible={true}
    htmlVisible={false}
    cssVisible={false}
>

```html
<div class="waku">
    <p class="shirushi">おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    border: 2px solid #7a4b2a;
    padding: 20px;
    background-color: #fff7ed;
}

.shirushi {
    border: 1px solid #c2410c;
    padding: 4px;
    background-color: #fed7aa;
    color: #7c2d12;
}

.nedan {
    border: 2px solid #c2410c;
    padding: 8px;
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

このあと、**値段 → カード全体 → 大きさ → 枠線 → 内側の余白** の順に作ります。

</Section>

<Section title="一部分だけ色を変えてクラスを覚える" goal="HTML の class と CSS の `.名前` を使って、ページ内の1か所にだけスタイルを当てる方法を覚える">

ここから一緒に作業します。**「180円」だけ色を変えたい**場面から始めます。

### まずはHTMLを書く

クラスが付いていない素のHTMLを用意します。

<CodePreview
>

```html
<p>おすすめ</p>
<h2>チョコドーナツ</h2>
<p>ふんわり生地にチョコがけ。</p>
<p>180円</p>
```

```css
/* ここに CSS を書いていきます */
```

</CodePreview>

### `p` をそのまま選ぶと全部に当たる（やってはいけない方法）

タグ名 `p` にスタイルを書くと、**ページの中のすべての `<p>`** に当たります。

<CodePreview
>

```html
<p>おすすめ</p>
<h2>チョコドーナツ</h2>
<p>ふんわり生地にチョコがけ。</p>
<p>180円</p>
```

```css
p {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

:::caution[よくある間違い]
`p { ... }` のような **タグ指定** は、ページ内のすべての `<p>` を一括で変える方法です。**一部分だけを変えたいときは使わない**でください。
:::

「おすすめ」も説明文も「180円」も同じ色になりました。値段だけを変えたいときには困ります。

### 値段だけに class を付けて選ぶ（正しい方法）

HTMLで `class="nedan"` を付けた `<p>` にだけ CSS が当たるようにします。CSSでは、さっきの `p { ... }` を `.nedan { ... }` に書き換えます。

<CodePreview
>

```html
<p>おすすめ</p>
<h2>チョコドーナツ</h2>
<p>ふんわり生地にチョコがけ。</p>
<p class="nedan">180円</p>
```

```css
.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

**`.nedan` と書いたので、`class="nedan"` が付いた「180円」だけが変わります。**

### class と CSS の対応

<Concept title="class と `.名前` の対応">

- HTMLで `class="名前"` と付けた要素だけが対象になる。
- CSSでは `.名前` と書く（ `.nedan` のように、クラス名の前にドット `1つ`）。

つまり、**HTML側で付けた名前と、CSS側で `.` を付けた名前を、完全に同じにする**ことが部分的にスタイルを当てるコツです。

</Concept>

<Verify>「180円」だけ背景色と文字色が変わっていて、他の段落は素のままである</Verify>

</Section>

<Section title="カード全体を `.waku` で選ぶ" goal="`<div>` で複数の要素をまとめて、`.waku` という名前を付けてカード全体を選べるようにする">

`<div>` は、**いくつかの要素をまとめるためのタグ**です。カードにしたい部分を `<div>` で囲みます。

### 全体を `<div>` で囲む

<CodePreview
>

```html
<div>
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

### `<div>` に class="waku" を付ける

この `<div>` に `class="waku"` を付けます。**カード全体を選ぶための名前**です。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    background-color: #fff7ed;
}

.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

**背景色が付いた範囲が、`.waku` で選ばれているカード全体です。**

<Verify>カード全体の背景色が `.waku` のクラス名で塗られている</Verify>

</Section>

<Section title="カードの大きさを決める" goal="`width` と `height` を指定して、カードの横幅と高さを決める">

カードを四角に整えるために、**横幅**と**高さ**を指定します。

### 横幅を決める

`width` は **要素そのものの横幅** を決めます。背景色は要素全体に塗られるので、幅を変えると背景色の範囲も同じく広がります。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    background-color: #fff7ed;
}

.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

### 高さを決める

`height` は **要素そのものの高さ** を決めます。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    background-color: #fff7ed;
}

.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

### ボックスモデルの考え方

<Concept title="CSS のボックスモデル（4つの層）">

HTMLのすべての要素は、**「箱（ボックス）」** の形で表現されます。箱の中身は、内側から外側に向かって次の4つの層に分かれています。

```text
+-----------------------------------+
| margin（外側の余白）                |
|  +-----------------------------+   |
|  | border（枠線）              |   |
|  |  +-----------------------+   |   |
|  |  | padding（内側の余白） |   |   |
|  |  |  +-----------------+ |   |   |
|  |  |  | content         | |   |   |
|  |  |  | （内容・文字）  | |   |   |
|  |  |  +-----------------+ |   |   |
|  |  +-----------------------+   |   |
|  +-----------------------------+   |
+-----------------------------------+
```

内側から外側への順は、**「内容（content）→ 内側の余白（padding）→ 枠線（border）→ 外側の余白（margin）」** です。

:::note[重要]
このレッスンで指定する `width: 300px` と `height: 220px` は、**一番内側の「content（内容）」の大きさ**を決めます。`padding` と `border` はこの大きさには含まれず、あとから別で足されます。
:::

本レッスンでは `padding` と `border` を扱います。`margin` は次の [margin](./margin/index.mdx) で学びます。

</Concept>

<Verify>カードの横幅が `300px`、高さが `220px` に広がっている（背景色の範囲で確認）</Verify>

</Section>

<Section title="枠線と内側の余白を足す" goal="`border` と `padding` を足してカードを商品カードらしく仕上げる">

`padding` と `border` を `.waku` に足していきます。

### 枠線を足す

`border` は、**要素のまわりに枠線**をつけます。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    background-color: #fff7ed;
    border: 2px solid #7a4b2a;
}

.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

カードの外側に線が出ます。

### 内側に余白を足す

`padding` は、**内容と枠線のあいだの余白**です。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    background-color: #fff7ed;
    border: 2px solid #7a4b2a;
    padding: 20px;
}

.nedan {
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

**文字と枠線のあいだが広がります。**

### 値段にも枠線と余白を足す

同じ考え方で、値段にも `border` と `padding` を足します。`padding` の数字は見た目でちょうど良いものを採用しています（`.shirushi` は 4px、`.nedan` は 8px）。

<CodePreview
>

```html
<div class="waku">
    <p>おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    background-color: #fff7ed;
    border: 2px solid #7a4b2a;
    padding: 20px;
}

.nedan {
    border: 2px solid #c2410c;
    padding: 8px;
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

値段のまわりにも線と内側の余白が付きます。

<Verify>カードのまわりに枠線が出て、文字と枠線のあいだに余白ができている</Verify>

</Section>

<Section title="演習問題" goal="レッスンで学んだ4つのプロパティを、自分の手で書いて確認する">

### 演習1

<Exercise>

下の HTML に対して、CSSを書いてください。

**HTML**:

```html
<div class="waku">
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p>180円</p>
</div>
```

**CSS の条件**:

- `.waku` に対してスタイルを書く
- `width: 300px;`
- `height: 220px;`
- `border: 2px solid #7a4b2a;`
- `padding: 20px;`

**確認方法**:

- プレビュー領域で `.waku` の背景色の横幅が、横一杯より狭くなっている
- プレビュー領域で `.waku` の背景色の縦幅が、コンテンツより背が高くなっている
- 文字と枠線のあいだに余白ができている

この3点が目視で確認できればOKです。

<CodePreview
    share={false}
>

```html
<div class="waku">
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p>180円</p>
</div>
```

```css
/* ここに CSS を書いてください */

```

</CodePreview>

<Solution>

<CodePreview
>

```html
<div class="waku">
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p>180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    border: 2px solid #7a4b2a;
    padding: 20px;
}
```

</CodePreview>

**目視チェック**:

- [ ] `.waku` の横幅が 300px に広がっている（背景色の幅で確認）
- [ ] `.waku` の高さが 220px に広がっている（背景色の縦幅で確認）
- [ ] 文字と枠線のあいだに余白ができている

</Solution>

</Exercise>

### 演習2

<Exercise>

**HTML**:

```html
<div class="waku">
    <p class="shirushi">おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

値段でやったのと同じように、「おすすめ」にも `class="shirushi"` を付けます。HTMLで付けた名前は、CSSでは `.shirushi` と書いて選びます。

演習2では、演習1で使った `.waku` をベースにして、**目印（.shirushi）と値段（.nedan）の両方を含んだカード**を作ります。

**考えること**:

- カード全体は `.waku`
- 目印だけなら `.shirushi`
- 値段だけなら `.nedan`

**CSS の条件**:

- `.waku`
    - `width: 300px;`
    - `height: 220px;`
    - `border: 2px solid #7a4b2a;`
    - `padding: 20px;`
    - `background-color: #fff7ed;`
- `.shirushi`
    - `border: 1px solid #c2410c;`
    - `padding: 4px;`
    - `background-color: #fed7aa;`
    - `color: #7c2d12;`
- `.nedan`
    - `border: 2px solid #c2410c;`
    - `padding: 8px;`
    - `background-color: #ffedd5;`
    - `color: #7c2d12;`

<CodePreview
    share={false}
>

```html
<div class="waku">
    <p class="shirushi">おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
/* ここに CSS を書いてください */

```

</CodePreview>

<Solution>

<CodePreview
>

```html
<div class="waku">
    <p class="shirushi">おすすめ</p>
    <h2>チョコドーナツ</h2>
    <p>ふんわり生地にチョコがけ。</p>
    <p class="nedan">180円</p>
</div>
```

```css
.waku {
    width: 300px;
    height: 220px;
    border: 2px solid #7a4b2a;
    padding: 20px;
    background-color: #fff7ed;
}

.shirushi {
    border: 1px solid #c2410c;
    padding: 4px;
    background-color: #fed7aa;
    color: #7c2d12;
}

.nedan {
    border: 2px solid #c2410c;
    padding: 8px;
    background-color: #ffedd5;
    color: #7c2d12;
}
```

</CodePreview>

**目視チェック**:

- [ ] `.waku` の枠・背景色・余白が効いている
- [ ] `.shirushi` のスタイルが「おすすめ」**だけ**に効いている（他の `<p>` に同じスタイルが漏れていない）
- [ ] `.nedan` のスタイルが「180円」**だけ**に効いている

</Solution>

</Exercise>

</Section>

<Section title="完成チェックと困ったとき" goal="自力で書いたカードが要件どおりに仕上がっているか、自分の目で確認する">

<Checkpoint>

- [ ] `.waku` の `width` と `height` でカードの大きさが決まっている
- [ ] `.waku` の `border` と `padding` で枠線と内側の余白ができている
- [ ] `.shirushi` は「おすすめ」**だけ**に効いている
- [ ] `.nedan` は「180円」**だけ**に効いている
- [ ] ブラウザで 4 つのプロパティがすべて反映されている

</Checkpoint>

まず、できたところを上から順に見直しましょう。

**動作の確認**:

- [ ] `.waku` に `width: 300px;` と `height: 220px;` がある
- [ ] `.waku` に `border` と `padding` がある
- [ ] `.shirushi` は「おすすめ」**だけ**に効いている
- [ ] `.nedan` は「180円」**だけ**に効いている
- [ ] ブラウザで見て、4つのプロパティ（width・height・border・padding）がすべて反映されている

**よくあるつまずき**:

- `class="waku"` と `.waku` の名前が同じですか（同じものを指しているか）
- `class="nedan"` と `.nedan` の名前が同じですか
- `class="shirushi"` と `.shirushi` の名前が同じですか
- CSSファイルを保存しましたか
- ブラウザを再読み込みしましたか
- `border` に `solid` を書いていますか（書き忘れると線が出ないことがあります）
- CSSに全角の記号（全角スペース・全角の `:` など）が混ざっていませんか

### AI に質問するときのコツ

:::tip[確認手順を聞く]
AIに「完成コードを全部書く」ように頼むと、丸ごと書き直されてしまい、何が違っていたか分からなくなります。**「どこを確認すればよいか」を手順として聞く**方が、自分で直すべき箇所が見つかります。
:::

次のプロンプトを AI に送ると、段階的な確認手順を教えてもらいやすくなります。

```text
次の HTML と CSS で、class、width、height、border、padding のどれかが思ったように反映されません。
完成コードを全部書くのではなく、どこを確認すればよいかを順番に教えてください。
```

:::note[使い方の流れ]
上のプロンプトのあとに、自分の HTML と CSS を貼り付けて送ると、**「.waku の `.waku` のスペルは正しいか」「border の省略形 `2px solid` が揃っているか」**のような段階的な確認手順を教えてもらいやすくなります。
:::

</Section>
