import advancedStylingCompleteZipUrl from './assets/advanced-styling-complete.zip';

# 発展スタイリング

## 前提

この授業では、[スタイリング](../css-styling-basics/index.mdx) で作成したHTML・CSSに対して、さらにスタイリングを**追加**していきます。

---

## 今回のゴール

- CSSでスタイリングを行い、**Figmaで作成したデザイン**（[Figmaデザインはこちら](https://www.figma.com/design/ZiOoQg0bRrdeAvOQiyBgO3/CSS-Advanced-Styling?node-id=0-1&t=pYxbhnfwFKjmReb7-1)）を忠実に再現して下さい。
- CSSは、既存の記述を変更・削除せず、追記のみで対応する。
- HTMLは、既存の構造・テキストは変更せず、class/id の追加のみ可。

---

:::note

**補足**
HTMLを修正するときは、既存の構造・テキストはそのままに、class/id の追加のみ行ってください。

:::

---

## 追加するCSSのプロパティ一覧

- `margin`
- `padding`
- `border`
- `width`
- `height`
- `font-size`
- `text-align`
- `line-height`

:::note

**補足**
`margin`、`padding`、`border` については、`margin-left` や `padding-top`、`border-bottom` のように、特定の方向だけ指定する方法もあります。必要に応じて使ってみても構いません。

:::

---

## 追加のポイント

- **HTMLは、既存の構造・テキストは変更せず、class/id の追加のみ可。**
- **CSSは、既存の記述を変更・削除せず、追記のみで対応する。**
- ピンポイントで装飾したい個所には、**クラス名やIDをHTMLに追加**しましょう。
- 追加したクラスやIDに対して、上記のプロパティだけを使って**Figmaデザインの特徴を再現**してください。

---

## 📥 完成例のダウンロード

授業終了時点での完成例ファイルをダウンロードできます：

<DownloadLink
  file={advancedStylingCompleteZipUrl}
  filename="advanced-styling-complete.zip"
>
  advanced-styling-complete.zip
</DownloadLink>

---
